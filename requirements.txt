# ============================================================
#  CryptoPredictions — requirements.txt
#  Python 3.9 or 3.10 recommended (3.10 confirmed working)
# ============================================================

# --- CORE (always install) -----------------------------------
numpy>=1.23,<2.0          # <2.0 required for TF/keras compat; >=1.23 for pandas 2.x
pandas>=1.5,<3.0
scikit-learn>=1.0
statsmodels>=0.13
scipy>=1.7
matplotlib
seaborn
pillow
tqdm
hydra-core>=1.3
omegaconf>=2.3
jsonlines
python-dateutil

# orbit-ml — Bayesian time-series (needs cmdstanpy + Stan compiler)
orbit-ml>=1.1.3

# frontend and live data fetching
streamlit
yfinance